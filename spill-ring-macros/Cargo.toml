[package]
name = "spill-ring-macros"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Derive macros for spill_ring"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full", "parsing"] }
quote = "1.0"
proc-macro2 = "1.0"
